<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Contact Form Submission</title>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #b39c84; color: white; padding: 20px; text-align: center; }
    .content { background-color: #f8f9fa; padding: 20px; }
    .field { margin-bottom: 15px; }
    .label { font-weight: bold; color: #b39c84; }
    .value { margin-top: 5px; }
    .message-box { background-color: white; padding: 15px; border-left: 4px solid #b39c84; margin: 15px 0; }
    .footer { background-color: #6c757d; color: white; padding: 15px; text-align: center; font-size: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2>New Contact Form Submission</h2>
      <p>PropertyBase Form Submission</p>
    </div>
    
    <div class="content">
      <h3>Contact Details</h3>
      
      <div class="field">
        <div class="label">First Name:</div>
        <div class="value"><%= @form_data[:first_name] || 'Not provided' %></div>
      </div>
      
      <div class="field">
        <div class="label">Last Name:</div>
        <div class="value"><%= @form_data[:last_name] || 'Not provided' %></div>
      </div>
      
      <div class="field">
        <div class="label">Email:</div>
        <div class="value"><%= @form_data[:email] || 'Not provided' %></div>
      </div>
      
      <div class="field">
        <div class="label">Mobile Phone:</div>
        <div class="value"><%= @form_data[:mobile_phone] || 'Not provided' %></div>
      </div>
      
      <div class="message-box">
        <div class="label">Message:</div>
        <div class="value" style="white-space: pre-wrap;"><%= @form_data[:message] || 'No message provided' %></div>
      </div>
      
      <div class="field">
        <div class="label">Submitted at:</div>
        <div class="value"><%= Time.current.strftime("%B %d, %Y at %I:%M %p") %></div>
      </div>
      
      <div class="field">
        <div class="label">Source:</div>
        <div class="value">Website Contact Form (PropertyBase)</div>
      </div>
    </div>
    
    <div class="footer">
      <p>This email was automatically generated from your website contact form.</p>
      <p>Please respond to the client directly using the email address provided above.</p>
    </div>
  </div>
</body>
</html>
